import styled from "styled-components";

const MenuList = styled.ul`
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 1.2rem;
  gap: 2em;
  letter-spacing: 1px;
`;

export { MenuList };
